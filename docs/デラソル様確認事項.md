# 制作会社への確認事項

## 1. 本番環境の接続情報について

本番サーバーへのデプロイ作業を進めるにあたり、以下の接続情報をご共有いただけますでしょうか。

- FTP/SFTP接続情報（ホスト名・ポート・ユーザー名・パスワード）
- SSH接続情報（可能であればご共有ください）
- データベース接続情報（ホスト名・DB名・ユーザー名・パスワード）
- サーバー上のWordPressインストールパス

→ 確認中です！分かり次第連絡します。

## 2. トップページ スライドのリンク先について

スライド内の各バナーにリンクが設定されているかと思いますが、現状では対応する詳細ページが存在しない項目があります。

- リンクなし（`href="#"` または `pointer-events: none`）で実装してよいか？
- それとも、別ページ（例：学校説明会一覧ページ等）へ遷移させる想定か？
→ 各イベントページへリンクさせます。リンク先ページは現在作成中です。

- カスタムフィールドで設定したURLへリンクさせる実装でよいか？
→ URLはなんでも構いません。

【対応方針】現在のスライドは briefing・event カスタム投稿の詳細ページ（パーマリンク）へ自動リンクする実装になっています。「作成中のイベントページ」が固定ページの場合は、カスタムフィールド（外部URLや固定ページ指定）で上書きできる形に変更します。詳細ページURLが自動で取れる場合はカスタムフィールド不要です。→ **イベントページが完成したら最終仕様を確認させてください。**

【対応済み】briefing・event の各投稿編集画面にリンク先URLカスタムフィールドを追加済み（briefing: `briefing_button_url` / `outside_button_url`、event: `event_link_url`）。未入力の場合は投稿詳細ページへ自動リンク。外部URLの場合は `target="_blank"` で別タブ表示（トップスライド・公開行事一覧ページ共通）。

## 3. トップページ スライド内「要予約」の表示について

スライド内に「要予約」という文言が含まれていますが、この意味・用途を教えてください。

- 学校説明会など予約が必要なイベントを示すラベルか？
→ はい、予約が必要なイベントを示します。予約不要のイベント時は「予約不要」のラベルが表示されて欲しいです。

- WordPressで動的に出し分ける必要があるか（カスタムフィールド対応が必要か）？
- それとも静的な表示として固定でよいか？
→ 各イベントページへは動的生成になると思います。

【対応方針】briefing・event カスタム投稿タイプに「要予約フラグ」カスタムフィールド（チェックボックス）を追加し、ONの場合「要予約」・OFFの場合「予約不要」ラベルをスライドに動的表示する形で実装します。briefing・event 両方に対応します。

【対応済み】briefing・event 両投稿タイプの編集画面に「要予約フラグ」チェックボックスを追加。トップページスライドに `info-tag bg-blue` クラスで「要予約」または「予約不要」ラベルを動的表示。event の場合は予約期間（`event_period`）もラベル横に表示。briefing の場合は要予約時のみ予約期間（`briefing_web_cancel_period`）を表示。

## 4. SNS用OGPについて

各ページのOGP画像・タイトル・説明文が納品物に含まれていません。

- OGP画像はご用意いただけるか？
- ページごとに異なるタイトル・説明文を設定する必要があるか、共通でよいか？
→ OGP画像は共通でいいです。画像は別途用意します。
　 説明文は不要です。ページタイトルが表示されれば結構です。

【対応方針】`header.php` の `<head>` 内に共通OGPタグを実装します（`og:title` にページタイトル、`og:image` に共通画像、`og:description` は省略）。OGP画像が用意でき次第、テーマの `img/` フォルダに配置していただければ組み込みます。

【対応待ち】OGP画像のご用意をお待ちしています。画像ファイルを `cms/wp-content/themes/seibi/img/` に配置いただければ実装します。
←OGP画像をcms/wp-content/themes/seibi/img/に配置しました。
　トップページにのみ、<head>内にOGPタグを記入しました。
　全ページ同じで大丈夫です。
---

## 5. 未納品ページのHTMLについて

現在、以下のページがHTMLを受け取れておらず、プレースホルダー（枠のみ）の状態です。
HTMLが完成しましたらご共有ください。

### feature/ セクション（2026-03-25時点）

| ページ名 | URL | 状態 |
|---|---|---|
| 宗教教育 | /salesian/feature/religion/ | ✅移植完了 |
| 英語教育 | /salesian/feature/english/ | ✅移植完了 |
| 総合的な学習 | /salesian/feature/integrated-studies/ | ✅移植完了 |
| 宿泊学習 | /salesian/feature/stay/ | ✅移植完了 |
| 教科教育 | /salesian/feature/currciculum/ | ✅移植完了 |
| 進学実績 | /salesian/feature/career/ | ⏳HTML未納品 |
| アシステンツァ | /salesian/feature/assistenza/ | ⏳HTML未納品 |

### life/ セクション（2026-03-25時点）

| ページ名 | URL | 状態 |
|---|---|---|
| 星美クラスの一日 | /salesian/life/daily/ | ⏳HTML未納品 |
| 年間行事 | /salesian/life/year/ | ⏳HTML未納品 |
| 委員会・クラブ活動（特別音楽クラブ） | /salesian/life/activity/ | ⏳HTML未納品 |
| サレジアンアフタースクール | /salesian/life/after-school/ | ⏳HTML未納品 |

---

## 6. 「このサイトについて」「個人情報保護方針」のHTMLについて

以下の2ページはHTMLを受け取っておらず、現在プレースホルダーの状態です。
こちらのページは制作会社様の作成予定がありますでしょうか？

| ページ名 | URL |
|---|---|
| このサイトについて | /salesian/about/site/ |
| 個人情報保護方針 | /salesian/about/privacy/ |

---

## 8. FAQページ「家庭との連携･教育」リンク先について

[よくお寄せいただく質問](/salesian/about/faq/) の「その他」セクションに以下のQ&Aがあります。

> **Q. 保護者が参加する行事はどれくらいありますか。**
> A. 「家庭との連携･教育」のページをご参照ください。

この回答内に「家庭との連携･教育」ページへのボタンリンクが設置されていますが、該当ページがサイトマップに存在しません。納品HTMLでもリンク先が誤ったページを指していました。

- 「家庭との連携･教育」というページはサイトに含まれる予定でしょうか？
- 含まれる場合、どのセクション（URL）に配置しますか？
- 含まれない場合、Q&Aのテキスト・ボタンをどう修正しますか？（ボタン削除・別ページへ誘導など）

現在は既存の実装のまま保留しています。ご確認をお願いします。

---

## 7. 学校説明会・公開行事 詳細ページのデザインについて

`single-briefing.php`（学校説明会 詳細）・`single-event.php`（公開行事 詳細）のテンプレートを最小構成で作成済みです。
詳細ページのデザイン（HTMLファイル）をご用意いただければ組み込みます。

